name: Build Windows EXE

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  cleanup-artifacts:
    runs-on: ubuntu-latest
    steps:
      - name: Delete old artifacts (keep latest 3)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "=== 清理旧 artifact（保留最新3个）==="
          ARTIFACTS=$(gh api repos/${{ github.repository }}/actions/artifacts --jq '.artifacts[] | "\(.id)|\(.created_at)|\(.name)"' 2>/dev/null || true)
          COUNT=$(echo "$ARTIFACTS" | grep -c '|' 2>/dev/null || echo "0")
          echo "Found $COUNT artifacts"
          # 保留最新的3个，删除其余的
          echo "$ARTIFACTS" | sort -t'|' -k2 | head -n -3 | while IFS='|' read -r id date name; do
            echo "Deleting artifact: $name (id=$id)"
            gh api -X DELETE repos/${{ github.repository }}/actions/artifacts/$id || true
          done
          echo "=== Artifact 清理完成 ==="

  build-windows:
    needs: cleanup-artifacts
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Get version info
        id: version
        run: |
          $commit = $env:GITHUB_SHA.Substring(0, 7)
          echo "COMMIT_SHORT=$commit" >> $env:GITHUB_OUTPUT
          $date = Get-Date -Format "yyyyMMdd"
          echo "BUILD_DATE=$date" >> $env:GITHUB_OUTPUT

      - name: Build GUI EXE
        run: |
          pyinstaller --onefile --windowed --clean `
            --name 图片转WSD `
            --collect-all core `
            --collect-all gui `
            --collect-all modes `
            --collect-all utils `
            --collect-all cairosvg `
            --collect-all cairocffi `
            --hidden-import potrace `
            --hidden-import PIL `
            --hidden-import PIL.Image `
            --hidden-import PIL.ImageTk `
            --hidden-import PIL.ImageDraw `
            --hidden-import cv2 `
            --hidden-import numpy `
            --hidden-import wsd_pure_builder `
            --hidden-import svg2wsd_core `
            --hidden-import wsd_records `
            --hidden-import wsd_parser `
            --hidden-import wsd_text `
            --hidden-import tikz_utils `
            --hidden-import cairosvg `
            --hidden-import cairocffi `
            --hidden-import cssselect2 `
            --hidden-import defusedxml `
            --hidden-import tinycss2 `
            --hidden-import pillow `
            --hidden-import vtracer `
            --hidden-import image_to_svg `
            app.py

      # 模板文件已内嵌到代码中，不再需要复制

      - name: Create release zip
        run: |
          $commit = "${{ steps.version.outputs.COMMIT_SHORT }}"
          $zipName = "svg2wsd-windows-$commit.zip"
          Compress-Archive -Path dist\* -DestinationPath $zipName
          echo "ZIP_NAME=$zipName" >> $env:GITHUB_ENV

      - name: Upload artifact (best effort)
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: svg2wsd-windows-${{ steps.version.outputs.COMMIT_SHORT }}
          path: ${{ env.ZIP_NAME }}
          retention-days: 1
          if-no-files-found: warn

      - name: Clean old release assets
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          Write-Host "=== 清理 continuous release 中的旧 assets ==="
          try {
            $assets = gh api repos/${{ github.repository }}/releases/tags/continuous --jq '.assets[] | "\(.id)|\(.name)"' 2>$null
            if (-not $assets) {
              Write-Host "没有找到旧 assets（首次构建或 release 不存在）"
            } else {
              $assets | ForEach-Object {
                $parts = $_ -split '\|'
                $id = $parts[0]
                $name = $parts[1]
                Write-Host "删除旧 asset: $name (id=$id)"
                try { gh api -X DELETE repos/${{ github.repository }}/releases/assets/$id 2>$null } catch {}
              }
            }
          } catch {
            Write-Host "获取 release 信息失败（可能首次构建）: $_"
          }
          Write-Host "=== 旧 assets 清理完成 ==="

      - name: Upload to Release
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: continuous
          name: 最新构建
          body: |
            ## 图片转WSD 最新自动构建

            **版本**: v2.0.0
            **提交**: `${{ github.sha }}`
            **构建时间**: ${{ steps.version.outputs.BUILD_DATE }}
            **提交短哈希**: `${{ steps.version.outputs.COMMIT_SHORT }}`

            ### 下载说明

            请下载下方唯一的 `svg2wsd-windows-*.zip` 文件（每次构建会自动清理旧版本）。

            ### 启动失败排查

            如果双击后没有反应，请查看同目录下的 `svg2wsd_startup.log` 文件获取错误信息。

            ### 下载加速（国内用户）

            在下载链接前加上镜像地址即可加速：

            ```
            https://ghproxy.com/原始下载链接
            ```
          prerelease: true
          files: ${{ env.ZIP_NAME }}
          fail_on_unmatched_files: true
